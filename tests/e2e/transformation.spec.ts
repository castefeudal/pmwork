import {test,expect} from '@playwright/test';
import {demoWorkspace} from '../../src/data/demo';
import {route} from './support';
import AxeBuilder from '@axe-core/playwright';
test('guided project context persists and Today opens its exact record',async({page})=>{
 await page.goto(route('/en/workspace/'));await page.getByRole('button',{name:/Start my project/}).click();const d=page.getByRole('dialog');await d.getByLabel('Project title',{exact:true}).fill('Operating profile test');await d.getByLabel('Measurable outcome',{exact:true}).fill('Accept the pilot');await d.getByLabel('My role / owner',{exact:true}).fill('Project lead');await d.getByRole('button',{name:'Next',exact:true}).click();await d.locator('input[name="volatility"]').focus();await page.keyboard.press('End');await d.getByRole('button',{name:'Show recommendations'}).click();await expect(d).toContainText('Context compatibility');await expect(d).toContainText('Plan 1–2 weeks in detail');await d.getByRole('button',{name:'Create project and start',exact:true}).click();await expect(page.getByRole('heading',{name:'Today',exact:true}).first()).toBeVisible();
 await expect.poll(async()=>page.evaluate(()=>{const raw=localStorage.getItem('pmwork:workspace:v3');if(!raw)return null;const parsed=JSON.parse(raw);return (parsed.workspace??parsed).projectSettings?.[0]?.context?.volatility??null;})).toBe(5);
 await page.reload();await expect(page.getByRole('heading',{name:'Today',exact:true}).first()).toBeVisible();
 const stored=await page.evaluate(()=>JSON.parse(localStorage.getItem('pmwork:workspace:v3')!));expect((stored.workspace??stored).projectSettings[0].context.volatility).toBe(5);
});
test('Today top priority opens the referenced risk with a typed action',async({page})=>{const w=demoWorkspace('en');await page.addInitScript(w=>localStorage.setItem('pmwork:workspace:v3',JSON.stringify(w)),w);await page.goto(route('/en/workspace/?view=overview'));const top=page.locator('.priority-focus');await expect(top).toContainText('Top priority now');await top.getByRole('button').click();await expect(page.getByRole('dialog')).toBeVisible();await expect(page.getByRole('dialog').getByLabel('Title',{exact:true})).not.toHaveValue('');});
test('intent search returns playbook and scheduling tool',async({page})=>{await page.goto(route('/ru/'));const trigger=page.getByRole('button',{name:'Поиск',exact:true});await expect(trigger).toBeVisible();await trigger.click();const search=page.getByRole('searchbox');await expect(search).toBeVisible();await search.fill('проект опаздывает');await expect(page.locator('.command-results')).toContainText('Проект опаздывает');await expect(page.locator('.command-results a[href*="tool=cpm"]')).toBeVisible();});
test('deadline input validation and deterministic forecast are accessible',async({page})=>{await page.goto(route('/en/tools/?tool=deadline'));await page.getByRole('button',{name:'Calculate',exact:true}).click();await expect(page.getByRole('status')).toContainText('Check inputs');await page.getByLabel('Completions per full calendar day, including zeros, comma separated').fill(Array(14).fill(1).join(','));const target=new Date(Date.now()+20*86400000).toISOString().slice(0,10);await page.getByLabel('Target date',{exact:true}).fill(target);await page.getByRole('button',{name:'Calculate',exact:true}).click();await expect(page.locator('.tool-output')).toContainText('100.0%');const axe=await new AxeBuilder({page:page as never}).withTags(['wcag2a','wcag2aa','wcag21aa','wcag22aa']).analyze();expect(axe.violations).toEqual([]);});
test('deadline scenario saves without creating an invalid project reference',async({page})=>{const w=demoWorkspace('en');await page.addInitScript(w=>localStorage.setItem('pmwork:workspace:v3',JSON.stringify(w)),w);await page.goto(route('/en/tools/?tool=deadline'));await page.getByRole('button',{name:'Choose local project',exact:true}).click();await page.getByRole('button',{name:'Use project data',exact:true}).click();await page.getByLabel('Completions per full calendar day, including zeros, comma separated').fill(Array(14).fill(1).join(','));const target=new Date(Date.now()+20*86400000).toISOString().slice(0,10);await page.getByLabel('Target date',{exact:true}).fill(target);await page.getByRole('button',{name:'Calculate',exact:true}).click();await page.getByRole('button',{name:'Save scenario',exact:true}).click();await expect(page.getByRole('status').filter({hasText:'Forecast snapshot saved and linked to the project.'})).toBeVisible();const stored=await page.evaluate(()=>JSON.parse(localStorage.getItem('pmwork:workspace:v3')!));const workspace=stored.workspace??stored;expect(workspace.toolRuns.some((run:{tool:string;appliedRecordIds:string[]})=>run.tool==='deadline'&&run.appliedRecordIds.length===0)).toBe(true);});
test('v5 migration preserves project records in browser',async({page})=>{const w={...demoWorkspace('en'),schemaVersion:5};await page.addInitScript(w=>localStorage.setItem('pmwork:workspace:v3',JSON.stringify(w)),w);await page.goto(route('/en/workspace/?view=overview'));await expect(page.locator('.workspace-shell')).toBeVisible();await expect.poll(async()=>page.evaluate(()=>{const s=JSON.parse(localStorage.getItem('pmwork:workspace:v3')!);return (s.workspace??s).schemaVersion})).toBe(6);const stored=await page.evaluate(()=>{const s=JSON.parse(localStorage.getItem('pmwork:workspace:v3')!);return (s.workspace??s).workItems.length});expect(stored).toBe(w.workItems.length);});
test('static English document exposes language before scripts',async({request})=>{const response=await request.get(route('/en/workspace/'));expect(await response.text()).toContain('<html lang="en"');});
test('project-connected EVM loads recorded budget and distinguishes missing earned value',async({page})=>{const w=demoWorkspace('en');await page.addInitScript(w=>localStorage.setItem('pmwork:workspace:v3',JSON.stringify(w)),w);await page.goto(route('/en/tools/?tool=evm'));await page.getByRole('button',{name:'Choose project data'}).click();await page.getByRole('button',{name:'Use project data',exact:true}).click();await expect(page.getByLabel('BAC',{exact:true})).toHaveValue(String(w.budgets.filter(x=>x.projectId==='atlas').reduce((s,x)=>s+x.planned,0)));await expect(page.getByLabel('EV',{exact:true})).toHaveValue('0');await expect(page.getByRole('status')).toContainText('enter measured values');});
test('playbook creates a linked issue and opens its editor',async({page})=>{const w=demoWorkspace('en');await page.addInitScript(w=>localStorage.setItem('pmwork:workspace:v3',JSON.stringify(w)),w);await page.goto(route('/en/playbooks/?q=Project%20is%20late'));const card=page.locator('article').first();await card.getByRole('button',{name:'Apply to a project issue'}).click();const dialog=page.getByRole('dialog');await dialog.getByLabel('Issue title').fill('Schedule recovery issue');await dialog.getByRole('button',{name:'Create issue',exact:true}).click();await card.getByRole('link',{name:'Open issue'}).click();await expect(page.getByRole('dialog').getByLabel('Title',{exact:true})).toHaveValue('Schedule recovery issue');});
test('MARKOVMADE saves and applies a deterministic result to project data',async({page})=>{const w=demoWorkspace('en');await page.addInitScript(w=>localStorage.setItem('pmwork:workspace:v3',JSON.stringify(w)),w);await page.goto(route('/en/tools/?tool=markovmade'));await page.getByRole('button',{name:'Choose local project'}).click();await page.getByLabel('Main constraint').fill('Slow validation feedback');await page.getByLabel('Candidate action').fill('Run a five-user validation');await page.getByLabel('What not to do yet').fill('Build the full workflow');await page.getByLabel('Validation metric').fill('Four of five users complete the task');await page.getByRole('button',{name:'Calculate priority'}).click();await expect(page.locator('.result-box')).toContainText('/100');await page.getByRole('button',{name:'Create Work Item'}).click();await expect(page.getByRole('status')).toContainText('record created');await expect(page.getByRole('link',{name:'Open affected record'})).toBeVisible();});
for(const locale of ['ru','en'])for(const tool of ['deadline','emv','capacity','matrix','ownership','change','calibration','markovmade'])test(`new tool surface ${locale} ${tool}`,async({page},info)=>{await page.goto(route(`/${locale}/tools/?tool=${tool}`));await expect(page.getByRole('button',{name:locale==='ru'?'Выбрать локальный проект':'Choose local project'})).toBeVisible();expect(await page.evaluate(()=>document.body.scrollWidth<=innerWidth+1)).toBe(true);await page.screenshot({path:`test-results/new-tool-${locale}-${tool}-${info.project.name}.png`,fullPage:true});const axe=await new AxeBuilder({page:page as never}).withTags(['wcag2a','wcag2aa','wcag21aa','wcag22aa']).analyze();expect(axe.violations).toEqual([]);});
